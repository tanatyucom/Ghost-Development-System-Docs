# Git Adapter Registry Profile Attachment

The canonical registry profile is:

- `docs/standards/git_adapter_registry_profile.md`

Required capability IDs:

- `git.commit`
- `git.push`
- `git.tag.create`
- `git.tag.push`

All entries are `DESIGN_ONLY` for this Q.

This attachment records that the Registry requirement was satisfied without
creating a runtime registry database.
