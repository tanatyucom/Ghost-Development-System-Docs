# Notes

- The first uploaded package contained mojibake and correctly triggered SCW.
- The UTF-8 BOM replacement resolved the exact resume condition.
- Startup repository state matched the Q: clean `main`, tracking `origin/main`.
- Existing approval runtime documents remain authoritative for runtime states;
  this Q adds classification and developer-friction policy.
- Runtime automation and automatic Commit/Push remain out of scope.
